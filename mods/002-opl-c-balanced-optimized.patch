diff --git a/src/opl.c b/src/opl.c
index a6d8fa3..87b08b2 100644
--- a/src/opl.c
+++ b/src/opl.c
@@ -1699,6 +1699,8 @@ static void setDefaults(void)
     gHDDPrefix = "pfs0:";
     gBaseMCDir = "mc?:OPL";
 
+    // Caches CDVD mantidos no default oficial (16/8/16).
+    // Pesquisa PSX-Place confirma: valores maiores deixam ZSO mais lento.
     bdmCacheSize = 16;
     hddCacheSize = 8;
     smbCacheSize = 16;
@@ -1735,23 +1737,24 @@ static void setDefaults(void)
     gHDDSpindown = 20;
     gScrollSpeed = 1;
     gExitPath[0] = '\0';
-    gDefaultDevice = APP_MODE;
+    // Device default = USB (universal, todo PS2 tem porta USB)
+    gDefaultDevice = BDM_MODE;
     gAutosort = 1;
-    gAutoRefresh = 0;
+    gAutoRefresh = 1;
     gEnableDebug = 0;
     gPS2Logo = 0;
-    gHDDGameListCache = 0;
-    gEnableWrite = 0;
-    gRememberLastPlayed = 0;
+    gHDDGameListCache = 1;
+    gEnableWrite = 1;
+    gRememberLastPlayed = 1;
     gAutoStartLastPlayed = 9;
     gSelectButton = KEY_CIRCLE; // Default to Japan.
     gBDMPrefix[0] = '\0';
     gETHPrefix[0] = '\0';
-    gEnableNotifications = 0;
-    gEnableArt = 0;
-    gWideScreen = 0;
-    gEnableSFX = 0;
-    gEnableBootSND = 0;
+    gEnableNotifications = 1;
+    gEnableArt = 1;
+    gWideScreen = 1;
+    gEnableSFX = 1;
+    gEnableBootSND = 1;
     gEnableBGM = 0;
     gSFXVolume = 80;
     gBootSndVolume = 80;
@@ -1760,7 +1763,8 @@ static void setDefaults(void)
     gXSensitivity = 1;
     gYSensitivity = 1;
 
-    gBDMStartMode = START_MODE_DISABLED;
+    // USB ativo no boot; HDD/ETH/APP desativados (reativáveis via Settings)
+    gBDMStartMode = START_MODE_AUTO;
     gHDDStartMode = START_MODE_DISABLED;
     gETHStartMode = START_MODE_DISABLED;
     gAPPStartMode = START_MODE_DISABLED;
